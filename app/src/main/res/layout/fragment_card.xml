<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.ui.main.card.CardFragment">

    <!-- Top Toolbar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/cardToolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:backgroundTint="?attr/colorPrimary"
        android:title="Cards"
        android:titleTextColor="@android:color/white"
        android:elevation="4dp"
        app:titleCentered="true"
        app:navigationIconTint="@android:color/white" />

    <!-- Main Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_marginTop="?attr/actionBarSize"
        android:padding="16dp"
        android:background="@color/md_theme_background">

        <!-- Card ViewPager -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/cardViewPager"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginTop="8dp" />

        <!-- Tab Indicators -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/cardTabLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            app:tabIndicatorColor="@color/primaryColor"
            app:tabSelectedTextColor="@color/primaryColor"
            app:tabTextColor="@android:color/darker_gray"
            app:tabIndicatorHeight="3dp"
            app:tabIndicatorGravity="center" />

        <!-- Enhanced Switches -->
        <com.google.android.material.materialswitch.MaterialSwitch
            android:id="@+id/switchWithdraw"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enable Withdraw"
            android:layout_marginTop="24dp"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            app:thumbTint="@color/primaryColor"
            app:trackTint="@color/md_theme_secondaryContainer" />

        <com.google.android.material.materialswitch.MaterialSwitch
            android:id="@+id/switchInternational"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enable International Transactions"
            android:layout_marginTop="16dp"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            app:thumbTint="@color/primaryColor"
            app:trackTint="@color/md_theme_secondaryContainer" />
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
